import React from 'react';
import ReactDOM from 'react-dom/client';
import { GifBucketApp } from './GifBucketApp';
import './styles.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <GifBucketApp />
  </React.StrictMode>,
)
